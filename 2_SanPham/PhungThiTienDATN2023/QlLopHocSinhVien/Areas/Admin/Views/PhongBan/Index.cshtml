@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@model IEnumerable<QlLopHocSinhVien.Model.Entities.PhongBan>
@{
    //ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<div class="card">
    <h5 class="card-header">DANH SÁCH @ViewBag.Title</h5>

    <div class="card-body p-1">
        <form asp-action="Index" method="get">
            <div class="row">
                <div class="col-md-6">
                    <div class="col-md-3">
                        <label class="form-label">Show entries</label>
                        <input type="number" name="pagesize" class="form-control form-control-sm" value="10" min="10" step="5" onkeydown="return false;">
                    </div>
                </div>

                <div class="col-md-6">
                    <div class="row">
                        <div class="col-12">
                            <label class="form-label">Từ khóa</label>
                            <div class="input-group">
                                <input type="text" name="keyword" class="form-control form-control-sm">
                                <button type="submit" class="btn btn-success btn-sm p-1"><i class='bx bx-search-alt-2'></i></button>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </form>
    </div>
    <div class="table-responsive text-nowrap">
        <p class="text-danger">@ViewBag.Message</p>
        <table class="table table-hover table-bordered">
            <thead>
                <tr class="text-center">
                    <th><a class="btn btn-success btn-sm p-1" asp-action="Create"><i class='bx bx-plus-medical'></i></a></th>
                    <th>Mã phòng ban</th>
                    <th>Tên phòng ban</th>
                    <th>Nội dung</th>
                    <th>Mô tả</th>
                </tr>
            </thead>
            <tbody class="table-border-bottom-0">
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            <div class="d-flex">
                                <a class="btn btn-primary btn-sm p-1 mx-1" asp-action="Edit" asp-route-id="@item.MaPb" title="Sửa"><i class="bx bx-edit-alt"></i></a>
                                <a class="btn btn-danger btn-sm p-1" asp-action="Delete" asp-route-id="@item.MaPb" title="Xóa"><i class="bx bx-trash"></i></a>
                            </div>
                        </td>
                        <td>@item.MaPb</td>
                        <td>@item.TenPb</td>
                        <td style="white-space:pre-wrap;">@item.NoiDung</td>
                        <td style="white-space:pre-wrap;">@item.MoTa</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>